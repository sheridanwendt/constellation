# Config

Configuration files for Constellation infrastructure services.

Currently:

- `nats/nats-server.conf` — JetStream configuration for the NATS event bus.

Phase 2 will add platform service configuration here as the API Gateway and
other platform services are introduced (see `docs/05-project-plan.md`).
