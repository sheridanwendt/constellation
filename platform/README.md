# Platform

Reserved for Constellation's core platform services (API Gateway, Event
contracts, Memory API, Skill API, Agent Registry, Permission model).

These are Phase 2 deliverables per `docs/05-project-plan.md` and
`docs/03-architecture.md`. Phase 1 provides only the underlying
infrastructure (PostgreSQL, Qdrant, NATS) these services will run on.
